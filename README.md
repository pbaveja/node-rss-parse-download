# node-rss-parse-download
Parses RSS and downloads podcast episodes in an OUTPUT folder

#### Requires
`data.json` file containing an object with a podcasts array

Each podcast being an object with `title` and `rss`
